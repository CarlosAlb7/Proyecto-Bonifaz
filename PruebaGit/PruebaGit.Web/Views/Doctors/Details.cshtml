@model PruebaGit.Web.Models.Doctor

@{
    ViewBag.Title = "Details";
}

<div class="container-fluid text-light" style="font-family:'Californian FB'">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card bg-primary">
                <div class="card-body text-lg-center ">
                    <h1>Doctor</h1>
                    <hr />
                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.Nombre_Doctor)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.Nombre_Doctor)
                        </dd>

                    </dl>
                </div>
                <div class="form text-center p-3">
                    @Html.ActionLink("Editar", "Edit", new { id = Model.Id }, new { @class = "text-light btn btn-success mr-5" })

                      <a href="@Url.Action("Index","Doctors")" class="btn btn-primary">  Inicio</a>
                    </div>
                </div>

            </div>
        </div>
    </div>

